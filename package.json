{
  "name": "citadel-tarot",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://sebbit0419.github.io/citadel-tarot",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "encrypt": "pagecrypt build/index.html build/index.html 0101",
    "predeploy": "npm run build && npm run encrypt",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}